{{- if gt (len .Values.frontend.config) 0 -}}
{{- $fullName := include "generic-three-tier.frontend.fullname" . -}}
apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    app: {{ template "generic-three-tier.name" . }}
    chart: {{ template "generic-three-tier.chart" . }}
    release: {{ .Release.Name }}
    component: frontend
    heritage: {{ .Release.Service }}
  name: {{ $fullName }}
data:
{{- range .Values.frontend.config }}
  {{ .name }}: |
{{ .content | indent 4 }}
{{- end }}
{{- end }}
