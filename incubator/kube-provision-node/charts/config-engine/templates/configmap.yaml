apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "fullname" . }}-code
  labels:
    app: {{ template "fullname" . }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
data:
  server.py: |
{{ .Files.Get "config/server.py" | indent 4 }}
